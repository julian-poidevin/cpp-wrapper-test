# CMakeLists.txt for src/libapi

# Define the library
add_library(libapi SHARED libapi.cpp)

# Optionally, you can also set additional properties
# set_target_properties(libapi PROPERTIES POSITION_INDEPENDENT_CODE ON)
